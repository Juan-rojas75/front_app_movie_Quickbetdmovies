// import { fetchPopularMovies } from "@/lib/api/api-movies";

export default async function Home(){
    // const dataMovie = await fetchPopularMovies()
    // console.log(dataMovie)
    return (
        <h1>Welcome to the FAVORITES!</h1>
    );
}